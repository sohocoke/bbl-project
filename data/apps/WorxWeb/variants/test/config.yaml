targets: .*-(etit.*|adit)  # all environments where test cert label should be used.


manifest_values:

  ncgapplication:
    description: WorxWeb with TEST certificate label.

  policies:

    policyValue[policyName='CertificateLabel']: TEST
