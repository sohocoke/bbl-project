targets: ch-etit,am-.*

manifest_values:

  ncgapplication:
    description: WorxMail for BRA Exchange infrastructure

  policies:
    policyValue[policyName='ExchangeServer']: '{var:exchange_server_address_bra}'
    policyValue[policyName='MailDomain']: '{var:mail_domain_bra}'
    policyValue[policyName='BackgroundServices']: '{var:background_services_address_bra}'

  role: WorxMail BRA