targets: (ch-etit|gbl-.*)

manifest_values:

  ncgapplication:
    description: WorxMail for GB exchange infrastructure

  policies:
    policyValue[policyName='ExchangeServer']: '{var:exchange_server_address_gb}'
    policyValue[policyName='MailDomain']: '{var:mail_domain_gb}'
    policyValue[policyName='BackgroundServices']: '{var:background_services_address_gb}'

  role: WorxMail GB