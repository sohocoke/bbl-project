id: WorxMail-test

manifest_values:
  ncgapplication:
    description: stub description in app deployment manifest

  policies:
    policyValue[policyName='BackgroundServicesGateway']: '{var:netscaler_gateway_address}'

variants:
- id: WorxMail-test-eur
  bundle_id: com.citrix.mailEUR
  manifest_values:
    ncgapplication:
      description: stub description for EUR variant of WorxMail-test
